#!/bin/bash

sudo fdisk -l
echo "Select EFI Drive: (/dev/sda)"
read EFI
# echo $EFI

sudo pacman -S --noconfirm grub efibootmgr
sleep 2
sudo grub-install --target=i386-pc "/dev/$EFI"
sudo grub-mkconfig -o /boot/grub/grub.cfg
